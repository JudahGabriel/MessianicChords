import { css } from "lit";

export const footerStyles = css`
    :host {
        font-family: var(--subtitle-font);
    }

    a {
        color: var(--theme-color);
        text-decoration: none;
    }
`;